@model List<Mascot>

@{
    ViewBag.Title = "Champions (2013)";
}

<h1>Champions</h1>

@foreach (var mascot in Model)
{
    <div class="mascot-headshot">
        <div style="background-image: url('/Content/Images/MascotLarge/@(mascot.Slug).png');">
            <img src="/Content/Images/Team/@(mascot.Team.Slug).png" />
        </div>
        <h5 style="float:right;">@mascot.Team.League.Name League</h5>

        <h3>@mascot.Name</h3>
        <h4>@mascot.Team.Name</h4>
    </div>
}